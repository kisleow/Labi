# include <stdio.h>
# include <math.h>

int main()
{
float x0, y0;
printf("x0, y0: ");
scanf("%f %f", &x0, &y0);

float x1, y1;
printf("x1, y1: ");
scanf("%f %f", &x1, &y1);

float x, y;
printf("x, y: ");
scanf("%f %f", &x, &y);

if (x >= x0 && x <= x1 && y >= y0 && y <= y1) puts("yes");
else puts("no");

return 0;
}
